# UDT Initiatives Cycle

The `udt-initiatives` cycle is a first-class research cycle in the repository model.

This implementation introduces the canonical folders and planning contract for initiative/project mapping, but it does not yet add a governed `prompt.md` under `act/`.

Until a dedicated prompt contract is added through OpenSpec, use this cycle as the canonical place for initiative-specific planning, observations, and reflection artifacts.
