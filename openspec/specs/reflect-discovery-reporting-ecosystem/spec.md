# Spec: discovery-reporting-ecosystem

## Purpose

Records that discovery reporting no longer owns the structured ecosystem CSV or HTML exports.

## Requirements

No active requirements. Discovery reporting now produces `reflect/discovery/reporting/ecosystem.md`. Structured CSV and HTML exports belong to the rating reporting phase under `reflect/rating/reporting/`.
