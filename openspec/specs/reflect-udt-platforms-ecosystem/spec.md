# Spec: reflect-udt-platforms-ecosystem

## Purpose

Defines the flattened synthesized ecosystem summary for the `udt-platforms` thread.

## Requirements

### Requirement: UDT platforms ecosystem summary lives in reflect

The repository SHALL contain `reflect/udt-platforms-ecosystem.md` as the synthesized ecosystem summary for `udt-platforms`.

#### Scenario: Researcher opens platforms synthesis

- **WHEN** a researcher opens `reflect/`
- **THEN** `udt-platforms-ecosystem.md` is available as a direct file
