## REMOVED Requirements

### Requirement: Governed act prompts are contract manifests

**Reason**: Replaced by the phase-prefixed `act-prompt-manifest` capability.

**Migration**: Use `act-prompt-manifest` for the shared manifest contract for governed prompt files under `act/`.

### Requirement: Act prompt manifests explain contract purpose

**Reason**: Replaced by the phase-prefixed `act-prompt-manifest` capability.

**Migration**: Use `act-prompt-manifest` for required contract purpose comments.

### Requirement: Act prompt manifests distinguish run inputs

**Reason**: Replaced by the phase-prefixed `act-prompt-manifest` capability.

**Migration**: Use `act-prompt-manifest` for run input section behavior.

### Requirement: Act prompt manifests limit resolver and execution glue

**Reason**: Replaced by the phase-prefixed `act-prompt-manifest` capability.

**Migration**: Use `act-prompt-manifest` for resolver and execution glue behavior.

### Requirement: Act prompt bodies are minimal task invocations

**Reason**: Replaced by the phase-prefixed `act-prompt-manifest` capability.

**Migration**: Use `act-prompt-manifest` for minimal prompt body behavior.
