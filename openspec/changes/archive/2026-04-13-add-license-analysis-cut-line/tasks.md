## 1. Update prompt file

- [x] 1.1 In `prompts/license-analysis.md`, add `> Paste into your AI session from this line onwards.` immediately after the `---` horizontal rule that closes the usage header and before `## Prompt`, matching the position in `platform-discovery.md:12` and `platform-comparison.md:16`

## 2. Update spec

- [x] 2.1 Sync `openspec/specs/license-analysis-prompt/spec.md` with the MODIFIED requirement from the delta spec — add the cut-line separator sentence and the new scenario to the usage header requirement
