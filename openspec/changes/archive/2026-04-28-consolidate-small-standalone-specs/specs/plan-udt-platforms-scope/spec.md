# Spec Delta: plan-udt-platforms-scope

## Change Type

Retire capability

## Rationale

This capability only restates the scope-table contract for the same thread already governed by `udt-platforms-cycle`.
The scope table belongs more naturally to the thread-level mapping contract than to a separate file-specific capability.
