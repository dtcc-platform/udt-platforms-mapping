## 1. Spec

- [x] 1.1 Update `repo-prompt-markdown-format` to ban product-native source and citation handles.
- [x] 1.2 Require real Markdown links with URL targets when links are emitted.
- [x] 1.3 Keep the change scoped to the shared Markdown formatting contract.

## 2. Validation

- [x] 2.1 Run `openspec validate ban-product-native-source-handles --strict`.
- [x] 2.2 Run `openspec validate --all --strict`.
