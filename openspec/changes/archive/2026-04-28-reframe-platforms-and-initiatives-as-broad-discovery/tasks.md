## 1. Discovery Thread Inputs

- [x] 1.1 Remove `plan/udt-platforms/source-policy.md` from the live workflow and update any affected planning notes.
- [x] 1.2 Remove `plan/udt-initiatives/source-policy.md` from the live workflow and update any affected planning notes.

## 2. Prompt And Contract Reframing

- [x] 2.1 Update `act/udt-platforms/prompt.md` to remove the governed source-policy input while keeping lightweight evidence guidance appropriate for broad discovery.
- [x] 2.2 Update README wording so `udt-platforms` and `udt-initiatives` are described as broad global discovery threads and `udt-platform-comparison` remains the stricter evaluative stage.

## 3. Spec Cleanup

- [x] 3.1 Update baseline specs for `act-udt-platforms-prompt`, `ar-folder-layout`, `udt-platforms-cycle`, and `udt-initiatives-cycle` to remove source-policy requirements from the first two threads.
- [x] 3.2 Retire the baseline capabilities `plan-udt-platforms-source-policy` and `plan-udt-initiatives-source-policy`.

## 4. Verification

- [x] 4.1 Verify that no active plan, prompt, README, or baseline spec still requires `source-policy.md` for `udt-platforms` or `udt-initiatives`.
- [x] 4.2 Refresh prompt-status or equivalent verification output if the `udt-platforms` prompt changes during implementation.
