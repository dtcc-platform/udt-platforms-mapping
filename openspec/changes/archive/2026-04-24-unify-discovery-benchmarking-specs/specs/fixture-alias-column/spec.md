## REMOVED Requirements

### Requirement: Fixture tables include an optional Aliases column for known name variants
**Reason**: Replaced by the unified `reflect-discovery-benchmarking` capability so alias semantics live with the benchmark fixture and eval workflow they govern.
**Migration**: Use `openspec/specs/reflect-discovery-benchmarking/spec.md` as the single baseline contract for the discovery benchmarking workflow.
