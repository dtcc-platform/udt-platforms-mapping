## 1. Scope Cleanup

- [x] 1.1 Remove `## Output Contract Reminder` and its table from `plan/udt-platforms/scope.md`.
- [x] 1.2 Confirm `plan/udt-platforms/scope.md` contains exactly one classification table with `Type`, `Definition`, and `Observable Criteria`.

## 2. Prompt Contract Ownership

- [x] 2.1 Confirm `act/udt-platforms/prompt.md` still contains the summary table output format.
- [x] 2.2 Confirm `act/udt-platforms/prompt.md` still states `Reason` semantics and platform-only comparison eligibility.

## 3. Verification

- [x] 3.1 Run OpenSpec validation or status checks for `move-output-contract-from-scope-to-prompt-spec`.
- [x] 3.2 Review changed files to ensure output-format instructions were removed only from scope, not from the prompt contract.
