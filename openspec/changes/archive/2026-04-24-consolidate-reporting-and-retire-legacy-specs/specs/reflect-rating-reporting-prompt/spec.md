## REMOVED Requirements

### Requirement: Rating reporting prompt file exists
**Reason**: Replaced by the unified `reflect-rating-reporting` capability so the prompt and its structured outputs are governed together.
**Migration**: Use `openspec/specs/reflect-rating-reporting/spec.md` as the single baseline contract for the rating reporting workflow.

### Requirement: Rating reporting prompt scans observe/rating automatically
**Reason**: Replaced by the unified `reflect-rating-reporting` capability so the prompt and its structured outputs are governed together.
**Migration**: Use `openspec/specs/reflect-rating-reporting/spec.md` as the single baseline contract for the rating reporting workflow.

### Requirement: Rating reporting prompt generates CSV and HTML outputs
**Reason**: Replaced by the unified `reflect-rating-reporting` capability so the prompt and its structured outputs are governed together.
**Migration**: Use `openspec/specs/reflect-rating-reporting/spec.md` as the single baseline contract for the rating reporting workflow.
