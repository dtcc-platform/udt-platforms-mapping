## REMOVED Requirements

### Requirement: Rating reporting owns ecosystem CSV and HTML outputs
**Reason**: Replaced by the unified `reflect-rating-reporting` capability so ownership and generation stay in one workflow contract.
**Migration**: Use `openspec/specs/reflect-rating-reporting/spec.md` as the single baseline contract for the rating reporting workflow.

### Requirement: Rating ecosystem CSV is comparison-oriented
**Reason**: Replaced by the unified `reflect-rating-reporting` capability so ownership and generation stay in one workflow contract.
**Migration**: Use `openspec/specs/reflect-rating-reporting/spec.md` as the single baseline contract for the rating reporting workflow.

### Requirement: Rating ecosystem CSV omits Layer
**Reason**: Replaced by the unified `reflect-rating-reporting` capability so ownership and generation stay in one workflow contract.
**Migration**: Use `openspec/specs/reflect-rating-reporting/spec.md` as the single baseline contract for the rating reporting workflow.

### Requirement: Rating ecosystem export reflects the core-platform-only rating scope
**Reason**: Replaced by the unified `reflect-rating-reporting` capability so ownership and generation stay in one workflow contract.
**Migration**: Use `openspec/specs/reflect-rating-reporting/spec.md` as the single baseline contract for the rating reporting workflow.
