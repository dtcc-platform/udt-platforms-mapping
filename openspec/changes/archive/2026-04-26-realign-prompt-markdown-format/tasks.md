## 1. Realign the shared Markdown contract

- [x] 1.1 Update the baseline `prompt-markdown-format` spec wording to describe the live governed prompt set rather than a generic `prompts/` directory
- [x] 1.2 Verify that current prompt specs which depend on the shared Markdown contract still read correctly after the wording change

## 2. Verify prompt alignment

- [x] 2.1 Confirm that the live prompts using the shared Markdown contract still satisfy the updated wording
- [x] 2.2 Refresh any prompt-status reasoning if the shared-contract description changes how dependency checks are explained
