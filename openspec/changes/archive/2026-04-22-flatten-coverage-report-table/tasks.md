## 1. Update eval prompt

- [x] 1.1 Replace the per-tag `## Tag:` section template in Step 5 of `evals/discovery/run.md` with a single flat table template: `Platform | Layer | Tags | <model columns>`
- [x] 1.2 Update the grouping instruction in Step 5 to say rows are ordered as in the benchmark (no section headings)

## 2. Rewrite coverage report

- [x] 2.1 Rewrite `evals/discovery/reports/coverage.md` with the flat recall table (38 rows, Tags column, same model columns)
