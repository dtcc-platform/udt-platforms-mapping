## REMOVED Requirements

### Requirement: Platform dimensions scoring contains comparison rubrics

**Reason**: Platform comparison scoring behavior now belongs in the `platform-comparison-rubric` behavior spec. `plan/` is reserved for run inputs, not stable behavior contracts.

**Migration**: Use `openspec/specs/platform-comparison-rubric/spec.md` for comparison dimensions and scoring behavior.
