## REMOVED Requirements

### Requirement: Platform source policy governs comparison evidence

**Reason**: Platform comparison source and citation behavior now belongs in the `platform-source-policy` behavior spec. `plan/` is reserved for run inputs, not stable behavior contracts.

**Migration**: Use `openspec/specs/platform-source-policy/spec.md` for acceptable source and citation behavior.
