## 1. Update platform-discovery-prompt spec and prompt

- [x] 1.1 Sync the delta spec: merge the new requirement into `openspec/specs/platform-discovery-prompt/spec.md`
- [x] 1.2 Add a **Markdown Syntax Rules** section to `prompts/platform-discovery.md`, placed immediately before the Output Format section, covering permitted syntax, citation format, prohibited syntax, and whitespace rules

## 2. Update platform-comparison-prompt spec and prompt

- [x] 2.1 Sync the delta spec: merge the new requirement into `openspec/specs/platform-comparison-prompt/spec.md`
- [x] 2.2 Add a **Markdown Syntax Rules** section to `prompts/platform-comparison.md`, placed immediately before the Output Format section, covering permitted syntax, citation format, prohibited syntax, and whitespace rules

## 3. Update license-analysis-prompt spec and prompt

- [x] 3.1 Sync the delta spec: merge the new requirement into `openspec/specs/license-analysis-prompt/spec.md`
- [x] 3.2 Add a **Markdown Syntax Rules** section to `prompts/license-analysis.md`, placed immediately before the Output Format section, covering permitted syntax, citation format, prohibited syntax, and whitespace rules
