## REMOVED Requirements

### Requirement: Entity discovery benchmark report stores benchmark output

**Reason**: The separate benchmark report is retired. Known candidate recall is now reported inside each entity discovery observation.

**Migration**: Use the `## Known Candidate Recall Check` section in `observe/entity-discovery-<model-short>.md`.
