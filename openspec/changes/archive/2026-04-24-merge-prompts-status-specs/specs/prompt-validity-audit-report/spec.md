## REMOVED Requirements

### Requirement: Prompt-validity audit report has a structured summary
**Reason**: Replaced by the unified `workflow-prompts-status` capability so the audit and its report are governed together.
**Migration**: Use `openspec/specs/workflow-prompts-status/spec.md` as the single baseline contract for the prompt-status workflow.

### Requirement: Report includes detailed findings for each prompt
**Reason**: Replaced by the unified `workflow-prompts-status` capability so the audit and its report are governed together.
**Migration**: Use `openspec/specs/workflow-prompts-status/spec.md` as the single baseline contract for the prompt-status workflow.

### Requirement: Report records ignored runtime-input churn correctly
**Reason**: Replaced by the unified `workflow-prompts-status` capability so the audit and its report are governed together.
**Migration**: Use `openspec/specs/workflow-prompts-status/spec.md` as the single baseline contract for the prompt-status workflow.
