## REMOVED Requirements

### Requirement: Comparison prompt output uses portable Markdown syntax
**Reason**: Duplicate of "Comparison prompt enforces agent-agnostic output structure", which is the superset requirement. Citation format (the only additional item) is already covered by the agent-agnostic requirement's citation format entry.
**Migration**: No behaviour change. Coverage is fully retained in "Comparison prompt enforces agent-agnostic output structure".
