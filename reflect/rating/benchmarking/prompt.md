# Rating Benchmarking Eval

> This prompt is a stub. It will be generated via the OpenSpec workflow when the rating benchmarking eval is designed.

When complete, this prompt will instruct Claude Code to:

1. Read `reflect/rating/benchmarking/benchmark.md` and extract all expected platforms
2. Glob all files matching `observe/rating/*.md`
3. Check recall per model and write a coverage report to `reflect/rating/benchmarking/coverage.md`
