# Rating Reporting Prompt

> This prompt is a stub. It will be generated via the OpenSpec workflow when the rating reporting prompt is designed.

When complete, this prompt will instruct Claude Code to:

1. Scan all files in `observe/rating/`
2. Extract platform rating data and produce `ecosystem.csv` and `ecosystem-map.html`
3. Save outputs to `reflect/rating/reporting/`
