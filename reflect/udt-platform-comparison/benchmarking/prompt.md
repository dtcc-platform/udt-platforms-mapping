# UDT Platform Comparison Benchmarking Eval

> This prompt is a stub. It will be generated via the OpenSpec workflow when the `udt-platform-comparison` benchmarking eval is designed.

When complete, this prompt will instruct Claude Code to:

1. Read `reflect/udt-platform-comparison/benchmarking/benchmark.md` and extract all expected platforms
2. Glob all files matching `observe/udt-platform-comparison/*.md`
3. Check recall per model and write a coverage report to `reflect/udt-platform-comparison/benchmarking/coverage.md`
